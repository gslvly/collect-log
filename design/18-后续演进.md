## 18. 后续演进

按实际需求逐步增加，不提前实现：

1. `number` 字段，映射为 `Nullable(Float64)`。
2. 批量上传接口。
3. 小时和天级聚合表。
4. 高频过滤字段的数据跳数索引。
5. 数据表 TTL。
6. 表级查看权限。
7. 严格幂等存储（`ReplicatedMergeTree` + Keeper，或独立幂等表）。
8. 多 Node 实例：把 SQLite 换成 PostgreSQL（表结构基本可平移）。
9. 高流量下的消息队列。

