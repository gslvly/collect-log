## 任务分配

派发任务时，如果文档中已经有了，那么直接告诉他做哪一块就行了，不必重复给信息。发布任务后不要轮训结果。

拆分批次前先估代码量：把每块涉及的**生产代码**与**测试**分别 `wc -l` 数出来，别凭感觉切。

**按功能纵切，不要按层横切。** 同一个特性的服务端与前端放进同一批做完，不要「先做完所有服务端、再做所有前端」。判据是：**每个批次落地后系统必须自身可用**。留下跨层不一致的中间态就是切错了——典型症状是前端的硬编码表与服务端新规则打架，前端自校验放行、服务端拒收，变成用户可见的损坏。

耦合度高的部分宁可一次做完。合并后体量过大时（生产代码上千行 + 测试上千行 + 前端上千行），**不要退回横切**，改成按特性切成多个各自端到端可上线的批次。

## 方案确认

当方案不可避免的需要使用非稳定方案时，必须告诉用户。比如用户让完成程序：“如果辱骂他人，就禁言”，实现用正则匹配。
